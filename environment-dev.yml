name: LearnAsYouGoEmulator-dev
channels:
  - defaults
  - conda-forge
dependencies:
  - numpy
  - matplotlib
  - pytorch
  - emcee=2.*     # Example is not compatible with emcee 3.*
  - scipy

  # Development
  - pre-commit
  - black
  - isort
  - flake8
  - mypy
  - pytest
